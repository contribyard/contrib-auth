require 'spec_helper'

RSpec.describe Contrib::Auth::Provider do
  describe '.sign_in_with_password' do
    context 'when credentials are correct' do

    end

    context 'when credentials are not correct' do
    end
  end
end
